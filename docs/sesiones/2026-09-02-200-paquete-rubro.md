# Bitácora — issue #200: paquete de rubro

El paquete reúne léxico, iconos, variantes, parámetros semilla, alertas y
pantallas como datos JSON. La cascada es período → estructura → empresa → rubro,
igual que parámetros de costeo; una clave ausente cae a defaults del núcleo.
