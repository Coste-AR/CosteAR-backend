# Sesión issue #278 — actualización de Vitest a 5

## Alcance

- Actualizar `vitest` y `@vitest/coverage-v8` a `^5.0.0` (con Vite compatible).
- Hacer construibles los mocks de `GroqService`, `CompanyService` y `VaultIndexerService`.
- Mover el mock de `correction-memory` al nivel superior, conservando su justificación.
- No tocar código de producción ni otras dependencias.

## Verificación

- Rojo intencional: `npm.cmd exec vitest run tests/application/advisor-service.test.ts` con `AdvisorService` devolviendo `null`: 1 archivo, 3 tests fallidos.
- Verde focalizado: 4 archivos, 21 tests pasados.
- `npm.cmd test`: 170 archivos pasados, 1 omitido; 1550 tests pasados, 4 omitidos.
- `npm.cmd run test:integration` con rol restringido `costear_app`: 18 archivos, 56 tests pasados.
- `npm.cmd run build`: correcto.
- `npm.cmd run test:e2e` con Redis local en `localhost:6379`: 1 archivo, 1 test pasado (1 omitido).

La primera corrida e2e no pudo iniciar porque no había Redis escuchando en el puerto esperado; se levantó un contenedor Redis efímero para repetirla.

## Fuera de alcance

- No se modifica código de producción.
- No se actualizan dependencias adicionales fuera de Vitest, coverage-v8 y el Vite compatible requerido por Vitest 5.
